/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // Esto habilita la estructura de rutas en Next.js 14+
  },
};

export default nextConfig;
